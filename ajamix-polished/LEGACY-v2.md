# LEGACY v2 snapshot — frozen

This directory is a read-only snapshot of the Almajirix PWA taken at commit `bdc727d`, prior to the v3.0 dual-track rework.

- **Not under active development.** No v3.0 features (dual-track routing, Use Today / Tomorrow Check, ad slots, `.ajamix` sharing, referral, KPI backend) are being propagated into this directory.
- **Not the deploy target.** Production deploys from [`../app/`](../app/) via [`../vercel.json`](../vercel.json).
- Kept in the repo as a labeled earlier-version reference, per the v3.0 build plan §6.2.

For the live v3.0 codebase, see [`../app/`](../app/).
