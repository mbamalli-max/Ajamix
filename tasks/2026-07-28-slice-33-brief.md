# Slice 33 brief — ajamix

Task ID:          2026-07-28-slice-33
Objective:        Finish slice-32: repoint the 359 **module** `imageCard` paths to `.webp` and update
                  `.gitignore`. Slice-32's WebP conversion is already complete and verified — do NOT redo it.

## Correction to slice-32's brief (this is why the previous slice stopped)

Slice-32's brief told you to also repoint `activities[].imageCard`. **That instruction was wrong.** Your
guard correctly caught it and stopping was the right call. Claude investigated and confirmed:

- The 6 activity records (`pn-maths-01..06`) reference `images/pn-maths-01.png`..`-06.png`.
- Those files **have never existed** — not on disk, not anywhere in git history (verified with
  `git rev-list --objects --all`).
- `activity.imageCard` is **never rendered as an image**. In `app/app.js` (~line 5698) it is used only as
  a truthy flag deciding whether to render a *text* card (`placeholder-media` div containing `topicHa` and
  Ajami text). There is no `<img src>` for activities. Compare `app/app.js` ~line 4663-4675, where
  **module** `imageCard` genuinely becomes an `<img src>`.

**Therefore: leave all 6 activity `imageCard` values EXACTLY as they are.** Rewriting them to `.webp`
would fabricate a reference to a file that still would not exist. They are pre-existing dead references
and are explicitly out of scope for this slice.

## Steps

1. **Amend `tools/image-pipeline/repoint-imagecards.mjs`** (it already exists from slice-32) so it
   processes **modules only** and ignores `activities` entirely. Keep every existing safety property:
   - Back up `app/content.json` to
     `ai-system/projects/ajamix/tasks/2026-07-28-pre-webp-content-389.json` before any write.
   - Assert the target `.webp` exists on disk for every path it rewrites; abort with no write if any is missing.
   - Rewrite only `imageCard` values matching exactly `images/<id>.png` → `images/<id>.webp`.
   - Preserve raw JSON formatting (exact string replacement, no reserialization).
   - After writing, re-parse and assert deep-equality with the pre-write object except for exactly the
     intended `imageCard` strings, and assert module count is still 389.
   - Idempotent: a second run is a clean no-op.
   - Add an explicit assertion that the 6 activity `imageCard` values are **byte-identical before and
     after** — this is now a guarded invariant, not merely an omission.

2. **Run it.** Report the number of modules rewritten (expect exactly 359).

3. **Update `.gitignore`**: replace the directory-wide `app/images/` line with `app/images/*.png` so the
   PNG masters stay untracked but the WebP files become trackable. Verify with `git status --short` that
   359 `.webp` files appear as untracked and **zero** `.png` files do. Report the exact counts.

4. **Verify** (report full output):
   - `node app/tools/validate-content.mjs` — 389 modules.
   - `node --test tools/audio-pipeline/qa-delivered-audio.test.mjs`.
   - Every module `imageCard` matches `^images/[^/]+\.webp$` and the referenced file exists on disk.
   - The 6 activity `imageCard` values still end in `.png` and are unchanged.

Note: Claude already grepped `app/app.js` and `app/sw.js` for hardcoded `.png` assumptions — the only
matches are unrelated app icons (`/app/icons/icon-192.png`, `icon-512.png`), and the module renderer takes
the path verbatim from `imageCard`. **No code change is needed or permitted in this slice.**

## Explicitly NOT in this slice

- Do NOT re-run or modify the WebP conversion — it is done (359 files, 812MB → 36.2MB, already verified).
- Do NOT delete or modify any PNG master or any `.webp` file.
- Do NOT touch `activities[]` in any way.
- Do NOT modify `app/app.js`, `app/sw.js`, or any other source file.
- Do NOT run any `git` command that changes state (read-only `git status` is fine).

---

In scope:         tools/image-pipeline/repoint-imagecards.mjs (amend)
                  app/content.json (module imageCard strings ONLY)
                  .gitignore (the app/images line only)
                  the report file

Out of scope:     Everything else. **No git state changes.**

Scope globs:
- tools/image-pipeline/repoint-imagecards.mjs
- app/content.json
- .gitignore
- tasks/2026-07-28-slice-33-report.md
- tasks/2026-07-28-slice-33-lastmsg.md
- tasks/2026-07-28-slice-33.jsonl

Known generated side effects:
- tools/audio-pipeline/_qa-report.json

Add-dir required:
- /Users/muhammadbamalli/Documents/New project/ai-system/projects/ajamix/tasks

Acceptance gates: node app/tools/validate-content.mjs && node --test tools/audio-pipeline/qa-delivered-audio.test.mjs && node -e "const fs=require('fs'); const c=JSON.parse(fs.readFileSync('app/content.json','utf8')); if(c.modules.length!==389) throw new Error('module count changed: '+c.modules.length); const bad=c.modules.filter(m=>m.imageCard && !/^images\/[^/]+\.webp\$/.test(m.imageCard)); if(bad.length) throw new Error(bad.length+' modules still non-webp'); const withCard=c.modules.filter(m=>m.imageCard); if(withCard.length!==359) throw new Error('expected 359 module image cards, got '+withCard.length); for(const m of withCard){ if(!fs.existsSync('app/'+m.imageCard)) throw new Error('missing file for '+m.id); } const acts=(c.activities||[]).filter(a=>a.imageCard); if(acts.length!==6) throw new Error('activity count changed'); for(const a of acts){ if(!/\.png\$/.test(a.imageCard)) throw new Error('activity imageCard was modified: '+a.activityId); } console.log('OK: 389 modules, 359 webp cards all present on disk, 6 activity png refs untouched');"

Context to read:  tools/image-pipeline/repoint-imagecards.mjs (existing, from slice-32),
                  tasks/2026-07-28-slice-32-report.md (what was already done),
                  app/content.json, .gitignore

Report to:        /Users/muhammadbamalli/Documents/ajamix/tasks/2026-07-28-slice-33-report.md

Constraints:      No new dependencies. No secrets. Stop and report rather than guess.

Rollback notes:   `app/content.json` restorable from the step-1 backup. `.gitignore` is a one-line change.
                  No image file is touched.

Report contents:  Modules rewritten (expect 359) · proof the 6 activity values are unchanged · `.gitignore`
                  before/after plus the git status counts · full acceptance-gate output · idempotency
                  re-run result · explicit confirmation no git state command was run.

Human approval gates: No commit, no push. Claude verifies, then Muhammad approves.
