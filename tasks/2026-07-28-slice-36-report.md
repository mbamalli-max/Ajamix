# Slice 36 report — Stage 3D–3G isolated Ajami prototype

Task: `2026-07-28-slice-36`

Status: **complete — prototype only; pending Muhammad's review before Stage 4**.

## Scope and safety baseline

- Normative source read first: `HAUSA_AJAMI_ORTHOGRAPHY_STANDARD.md`.
- Prior verified work read: `tasks/2026-07-28-slice-35-report.md`.
- Existing `tools/ajami-pipeline/` prototype is being extended, not duplicated or rewritten.
- Production paths are read-only for this task. No production runtime imports are being added.
- Nothing generated by this slice will be marked `approved`.
- No git state-changing commands and no new dependencies.

## Stage 3D — lexicon schema and seed

Complete.

- Added a dependency-free schema/validator with the exact eight allowed statuses:
  `candidate`, `provisional`, `rule_verified`, `human_reviewed`, `approved`,
  `ambiguous`, `blocked`, `deprecated`.
- The validator requires every minimum field, verifies normalized Boko and the
  exact Ajami code-point sequence, and enforces field types/enums.
- Any `approved` entry is rejected unless it has a non-empty `source`, non-empty
  `reviewer`, and valid `YYYY-MM-DD` `reviewDate`.
- Generated-origin validation rejects `approved` even when review-looking
  metadata is present. The generated-entry constructor only permits
  `candidate`, `provisional`, `ambiguous`, or `blocked`.
- Seed: 9 real-corpus words, all `candidate`/`provisional`, zero `approved`.
  Every complete spelling is explicitly described as unreviewed/provisional;
  canonical consonants do not imply approved word-level vowels.
- Round-trip/malformed tests: missing field, bad status, incomplete approval,
  and illegal generated promotion are all caught.
- Stage test result: **5 passed, 0 failed**.

## Stage 3E — candidate corpus and tokenizer coverage

Complete.

- Added a read-only extractor over the same ten Hausa-bearing field names used
  by the verified slice-35 audit.
- Output is self-labelled `candidate_corpus_not_gold`, `goldStandard: false`;
  every entry has `reviewStatus: candidate`.
- 26 normalized-word candidates cover all 31 required labels, including all
  fixed consonants, the four apostrophe/boundary classes, vowel/diphthong
  ambiguity, native vs Arabic lexical `h`, proper nouns, acronyms, numerals, and
  mixed-language contexts. Missing required labels: **0**.
- Identical normalized words are deduplicated. Each retains total
  `occurrenceCount` and up to 12 distinct references with source file, full
  field path, module ID, source word, and complete surrounding sentence.
- Proposed tokenization comes only from the existing verified tokenizer.
  Provisional Ajami uses `canonical-mapping.json` plus the standard's approved
  short-mark inventory as an explicitly incomplete review aid. The output
  retains completeness and unresolved-decision fields and cannot approve itself.
- Candidate extractor tests: **4 passed, 0 failed**.

### Full tokenizer/corpus statistics

- Hausa fields scanned: **7,227**
- Unique normalized words: **3,243**
- Known phoneme/vowel tokens: **977,529**
- Unknown-token occurrences: **1,294**
- Tokenization coverage: **99.8678%**
- Actual unknown tokens:
  - `P` U+0050: **936**
  - `p` U+0070: **178**
  - `x` U+0078: **78**
  - `q` U+0071: **48**
  - `V` U+0056: **37**
  - `v` U+0076: **12**
  - `ā` U+0101: **5**
- Fixed-consonant token occurrences:
  - `ɓ`: **1,184**
  - `ɗ`: **6,298**
  - `ƙ`: **6,391**
  - `ƴ` / apostrophe-`y` abstract token: **326**
  - `c`: **11,245**
  - `ts`: **4,150**
  - `sh`: **4,899**
  - `f`: **13,079**
  - `n`: **82,672**
  - plain `k`: **56,720**
  - plain `y`: **35,645**
- Provisional fallback produced at least a partial review string for **3,194**
  indexed normalized forms; **25/26** selected candidates have a partial
  fallback. A fallback is not a verified spelling.
- Arabic/Islamic-context heuristic: **138 unique words / 1,058 occurrences**.
- Mixed Hausa–English-context heuristic: **430 unique words / 2,937 occurrences**.
- Acronym candidates: **18 unique forms / 244 occurrences**.

### Apostrophe carry-over

- Total occurrences: **3,082**
- Settled apostrophe-`y`: **254**
- Still queued for review: **2,828**
- Classifier categories: glottal boundary **1,801**; ambiguous **837**;
  apostrophe-`y` **254**; paired quotation **150**; unpaired typographic
  punctuation **37**; morpheme boundary **3**.

## Stage 3F — Unicode hygiene validator

Complete.

- Added a non-mutating string validator plus recursive read-only
  `app/content.json` scanner. Every finding records file, full field path,
  entity ID, original string, full code-point sequence, violated rule, evidence
  positions, and recommended disposition.
- Synthetic tests cover presentation forms, PUA, U+FFFD, Latin+Arabic marks,
  marks without Arabic bases, mark order, NFC instability, constructed
  U+06DB substitutes, mixed canonical/noncanonical forms, new-output U+06D1,
  native-Hausa `ه` classification, unconverted special letters/apostrophe-`y`,
  and unsupported extended glyphs.
- `U+06D1` is blocked only in `newlyGenerated` mode. It is not mislabeled
  malformed during stored/import analysis.
- Live scan: **15,717 strings**, including **315 Ajami fields**.
- Findings: **646 rule findings across 289 fields**. These include confirmed
  malformed Unicode and review-required orthographic/context findings; the
  latter are not silently promoted to confirmed spelling errors.
- Rule counts:
  - mixed canonical/noncanonical native-Hausa output: **251 fields**
  - unsupported extended Arabic glyph: **85 fields**
  - invalid combining-mark order: **83 fields**
  - NFC instability: **83 fields**
  - native-Hausa `ه` requiring lexical review: **79 fields**
  - Arabic Presentation Forms: **37 fields**
  - combining mark without a valid Arabic base: **15 fields**
  - Latin letter carrying an Arabic combining mark: **13 fields**
- Unsupported extended glyph occurrences found: `ے U+06D2` **2**,
  `ڈ U+0688` **74**, `چ U+0686` **171**.
- No live findings for PUA, U+FFFD, U+06D1-as-new-output, constructed U+06DB,
  unconverted Boko hook letters, or unconverted apostrophe-`y`.
- Known-defect self-test passed:
  - entity `pn-maths-01`, `topicAjami`: presentation forms including the
    required U+FEDB, U+FECF, U+FED3, U+FBAD, U+FEE3 were detected.
  - entity `V08`: Latin `V` + fatha was detected in **13 fields / 20
    occurrences**.
- Source SHA-256 before and after the scan was identical:
  `31bbf5c6cafca196751175767aa873070ac6ecb726c0ff0f0fa913998134b22f`.
- Stage tests: **4 passed, 0 failed**.

## Stage 3G — font and rendering probe

Complete for static analysis; browser visual review remains the explicit human gate.

- Implemented a dependency-free WOFF2 directory parser, Node Brotli
  decompression, and parser for the shipped font's untransformed `cmap`
  format-4 table.
- WOFF2 parsing succeeded: 17 tables; decompressed transformed-table stream
  237,714 bytes; `cmap` 2,282 bytes, untransformed.
- Nominal `cmap` glyph verified (**10/15**):
  - `ݑ U+0751` glyph 257
  - `ط U+0637` glyph 55
  - `ث U+062B` glyph 43
  - `ک U+06A9` glyph 169
  - `ڟ U+069F` glyph 159
  - `ی U+06CC` glyph 204
  - `ؿ U+063F` glyph 63
  - `ح U+062D` glyph 45
  - `ه U+0647` glyph 71
  - `ݣ U+0763` glyph 275
- No nominal glyph in the shipped font's `cmap` (**5/15**):
  - `ࢼ U+08BC`
  - `ࢻ U+08BB`
  - `ࢽ U+08BD`
  - `ࣃ U+08C3`
  - `ࣄ U+08C4`
- This is a font/fallback architecture finding, not permission to substitute
  code points. No mapping was changed.
- What static analysis **cannot determine**: contextual joining and GSUB
  substitutions, GPOS mark placement, outline quality, visible tofu, or actual
  browser fallback. Those remain unverified.
- HTML probe loads the actual shipped font and renders all 15 exact targets in
  isolated/initial/medial/final contexts plus sukūn, shadda, and short-e. Each
  row includes a deliberately missing-font fallback control and a human-result
  selector.
- Stage tests: **3 passed, 0 failed**.

## Acceptance gates

All required gates passed on the final generated artifacts:

- `node --test tools/ajami-pipeline/`
  - **36 passed, 0 failed**
  - includes the pre-existing 20 tests plus 16 Stage 3D–3G tests
- `node app/tools/validate-content.mjs`
  - **OK — 389 modules pass**
- `git diff --quiet -- app/ docs/ ajamix-polished/ && echo PRODUCTION_UNCHANGED`
  - output: **`PRODUCTION_UNCHANGED`**
- `git diff --stat -- app/ docs/ ajamix-polished/`
  - **empty**
- `git diff --name-only -- app/ docs/ ajamix-polished/`
  - **empty**

The repository's Stage 2/3 prototype files are currently untracked, so ordinary
`git diff --stat` does not enumerate them. `git status --short
--untracked-files=all` identifies the additions under
`tools/ajami-pipeline/` and this report; no production path is listed as
modified. No git state-changing command was used.

No seed or generated candidate entry is `approved`. No production converter,
stored Ajami, `ajami_validated` value, learner rendering, setting, service
worker, manifest, or documentation file was changed.

## Files added

- `tasks/2026-07-28-slice-36-report.md` (this incremental report)
- `tools/ajami-pipeline/lexicon/schema.mjs`
- `tools/ajami-pipeline/lexicon/schema.test.mjs`
- `tools/ajami-pipeline/lexicon/seed-lexicon.json`
- `tools/ajami-pipeline/extract-candidates.mjs`
- `tools/ajami-pipeline/extract-candidates.test.mjs`
- `tools/ajami-pipeline/data/candidate-corpus.json`
- `tools/ajami-pipeline/validate-unicode.mjs`
- `tools/ajami-pipeline/validate-unicode.test.mjs`
- `tools/ajami-pipeline/data/unicode-validation-report.json`
- `tools/ajami-pipeline/font-probe.mjs`
- `tools/ajami-pipeline/font-probe.test.mjs`
- `tools/ajami-pipeline/font-probe.html`
- `tools/ajami-pipeline/data/font-probe-report.json`

## Existing prototype file extended

- `tools/ajami-pipeline/test-suite.mjs` — imports the 16 new tests so the exact
  directory acceptance command runs all **36** tests.

## Human gate / what remains deliberately unverified

- Open `tools/ajami-pipeline/font-probe.html` in a browser and record the visual
  joining, mark placement, tofu, and fallback results.
- Review candidate spellings, vowel length, carriers, lexical `h`, unresolved
  apostrophes, mixed-language routing, acronyms, and numerals.
- Nothing here authorizes Ajami reactivation, converter consolidation, content
  migration, or promotion to `approved`.

---

# Claude's independent verification (appended after Codex's report)

Postflight PASSED and `turn.completed` is present (unlike slice-35). Every material claim below was
re-derived by Claude directly, not accepted from the report.

## Production integrity — VERIFIED
`git diff --stat` empty across all tracked files. `grep -rn "ajami-pipeline" app/ docs/` → no hits;
prototype remains isolated. 25 files now under `tools/ajami-pipeline/`, purely additive.
Validator's own sha256 before/after on `content.json` matched (`31bbf5c6…`), and git confirms it.

## 3D lexicon safety gate — VERIFIED BY ADVERSARIAL TEST
Claude wrote hostile entries against `validateLexiconEntry()`:
- `status:"approved"` with no source/reviewer → **rejected** ("approved entries require at least one
  source", "…require a reviewer", "…require a reviewDate").
- Generated entry claiming `approved` → **rejected** ("generated entries may not have status approved").
- `createGeneratedEntry()` returns `status:"candidate"` — **cannot self-promote**.
- Seed lexicon: 9 entries, 8 `provisional` + 1 `candidate`, **zero `approved`**.

*Claude process note*: a first adversarial script threw `TypeError` on a guessed export name. Claude did
**not** count that as a passing safety check — found the real export (`validateLexiconEntry`) and re-ran.

## 3F Unicode validator — VERIFIED, and the corpus is in worse shape than assumed
Both known real defects are detected (self-test `passed: true`, independently confirmed by Claude):
`pn-maths-01.topicAjami` presentation forms; `V08` Latin "V"+fatha.

**646 findings across 289 of 315 stored Ajami fields — 92% of all stored Ajami has at least one Unicode
hygiene violation under the approved standard.** Top rules: mixed canonical/noncanonical (251),
unsupported extended glyph (85), invalid combining-mark order (83), NFC instability (83), unconditional
`ه` for native Hausa (79), presentation forms (37).

## 3G font probe — BLOCKING FINDING, independently confirmed
Codex's dependency-free WOFF2 parser reported 10/15 target glyphs present. **Claude independently
re-verified with `fontTools` (installed for this check) and got an identical result.**

**Missing from `app/fonts/NotoNaskhArabic-Regular.woff2`:**

| Code point | Boko | Status in standard |
|---|---|---|
| `U+08BC` | `ƙ` | **canonical** |
| `U+08BB` | `f` | **canonical** |
| `U+08BD` | `n` | **canonical** |
| `U+08C3` | `gw`/`gy` | provisional |
| `U+08C4` | `ƙw`/`ƙy` | provisional |

All five are `U+08xx` characters added in recent Unicode versions; the shipped Noto Naskh build predates
them. **The shipped font physically cannot render the approved standard.**

Impact measured by Claude against live Boko content: `n` occurs **82,672** times, `f` **13,079**, `ƙ`
**6,764**; **1,709 of 3,326 distinct Hausa words (51%) contain at least one affected letter.** Rendering
today would produce tofu/.notdef boxes across roughly half the vocabulary.

Per the standard's own rule, the fix is font/fallback architecture — **not** substituting different code
points. This is a Stage 4 blocker.

## Tokenizer coverage + corpus statistics — VERIFIED CONSISTENT
- 7,227 Hausa fields scanned · 3,243 unique normalized words · **tokenization coverage 99.87%**
- Unknown tokens 1,294, all non-Hausa Latin letters (`P` 936, `p` 178, `x` 78, `q` 48, `V` …) — i.e.
  English/loanword leakage, not tokenizer gaps.
- `HAUSA_GLOTTALIZED_Y` = **326**, exactly matching Claude's independent scan (254 apostrophe-`y` + 72
  `ƴ`/`Ƴ`). Cross-source consistency confirmed.
- Apostrophe carry-over identical to slice-35: 3,082 / 254 settled / 2,828 queued.
- Candidate corpus correctly self-labels `goldStandard: false`, `materialType:
  candidate_corpus_not_gold`; **zero entries marked `approved`.**

## Tests
`node --test tools/ajami-pipeline/` re-run by Claude: **36 passed, 0 failed.**

## Status
Prototype only. Nothing `approved`. Ajami mode disabled, Latin kill switch intact, production untouched.
**Stage 4 is blocked on the font finding above.**
